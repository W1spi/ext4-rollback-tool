# Base directory where docker snapshots are stored
DEST_BASE=/mnt/nextcloud_data/.infra_snapshots/docker

# How many snapshots to keep
KEEP_COUNT=28

# Docker infrastructure directory
DOCKER_PROJECTS_DIR=/opt/devteam/docker

# Docker daemon config directory
DOCKER_ETC_DIR=/etc/docker
